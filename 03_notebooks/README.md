# 03_notebooks

Exploratory notebooks only. Clear outputs before committing:

```bash
jupyter nbconvert --clear-output --inplace my_notebook.ipynb
```

Any code used more than once belongs in `01_scripts/` as a function.
