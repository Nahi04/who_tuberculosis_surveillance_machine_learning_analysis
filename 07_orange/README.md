# 07_orange

Orange3 workflows. One file per person: `workflow_<name>.ows`. Never edit someone else's file in
parallel — `.ows` files are generated XML and merge badly.

Keep a short note here of what each workflow does, which widgets it uses and which data file it
expects, so the final integrative workflow can be assembled from them.
