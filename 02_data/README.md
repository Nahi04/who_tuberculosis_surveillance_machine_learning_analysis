# 02_data

`raw/` holds the data exactly as downloaded — never edit these files.
`processed/` holds cleaned tables produced by the code — regenerable, so not versioned.

Document every source below so that any team member can rebuild the folder from scratch.

| File | Source / URL | Downloaded on | By whom |
|---|---|---|---|
|  |  |  |  |
